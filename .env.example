# API Configuration
# Base URL for the God-Level Backend (e.g., Spring Boot server)
# It is used for both REST API endpoints and STOMP WebSockets (auto-converted to ws://)
VITE_API_URL=http://localhost:8080
